# Copy to .env for local use. Never commit .env.
# Placeholders only — names this snapshot actually reads. No real values.

# bots/03-05-spectrix/watchdog.py (historical ops helper; do not run live)
ALERT_PHONE=
DATABASE_URL=postgresql:///polybot
BOT_DIR=
PYTHON=
PG_ISREADY=
BREW=

# LLM client keys in bots/02-phantom/analysis/ensemble.py are constructor
# parameters, not env reads. If you fork, inject via constructor — never commit
# real values. Names below are documentation only.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GOOGLE_API_KEY=
